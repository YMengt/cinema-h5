export let filterName=(date,split=" ")=>{
    if(!date){
        return ''
    }else{
        let num=""
        data.actors.map((item)=>{
           num+=item.name
        })
        return num;
    }
}